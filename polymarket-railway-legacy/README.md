# Polymarket Railway Python stack (archived path)

These files were moved out of `railway-worker/` so the SQI Sovereign **Node** worker deploys on Railway without Python/Docker taking over the build.

Deploy Python separately if needed (different Railway service / root).
