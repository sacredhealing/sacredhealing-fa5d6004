# Railway: set the service **Root Directory** to `polymarket-copy-bot`.
# Variables: add PAPER_MODE=true (default also set in Dockerfile).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
