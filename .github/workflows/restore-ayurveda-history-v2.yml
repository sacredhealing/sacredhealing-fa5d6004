name: Restore All Ayurveda History
on:
  push:
    paths:
      - 'scripts/restore-ayurveda-history.py'
jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run restore
        run: python3 scripts/restore-ayurveda-history.py
