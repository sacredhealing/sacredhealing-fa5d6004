name: Run CLAWBOT SQL Direct

on:
  workflow_dispatch:

jobs:
  run-sql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ADMIN_PAT }}
      - name: Audit and fix PnL
        env:
          SK: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SK2: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GH_PAT: ${{ secrets.GH_ADMIN_PAT }}
        run: |
          python3 scripts/resolve_trades.py 2>&1 | tee _resolve_result.txt
          git config user.email "bot@sqi.app"; git config user.name "SQI-Bot"
          git add _resolve_result.txt && git commit -m "ci: pnl fix result" || true
          git push "https://x-access-token:${GH_PAT}@github.com/sacredhealing/sacredhealing-fa5d6004.git" main || true
