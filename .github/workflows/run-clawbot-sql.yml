name: Run CLAWBOT SQL Direct

on:
  workflow_dispatch:

jobs:
  run-sql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ADMIN_PAT }}
      - name: Check last trades
        env:
          SK: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SK2: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GH_PAT: ${{ secrets.GH_ADMIN_PAT }}
        run: |
          KEY="${SK:-$SK2}"
          BASE="https://fjdzhrdpioxdeyyfogep.supabase.co/rest/v1"
          LOG="_resolve_result.txt"
          
          curl -s "${BASE}/delta_arb_trades?select=asset,signal,delta,status,pnl_usdc,created_at&order=created_at.desc&limit=5" \
            -H "apikey: ${KEY}" -H "Authorization: Bearer ${KEY}" | python3 -c "
import sys,json,datetime
data=json.load(sys.stdin)
now=datetime.datetime.now(datetime.timezone.utc)
print(f'Last {len(data)} trades:')
for t in data:
    dt=datetime.datetime.fromisoformat(t['created_at'].replace('Z','+00:00'))
    ago=round((now-dt).total_seconds()/60,1)
    print(f'  {ago}min ago | {t.get("asset","?"):4} {t.get("signal","?"):5} {t.get("delta","?"):12} {t.get("status","?")}')
" | tee $LOG

          git config user.email "bot@sqi.app"; git config user.name "SQI-Bot"
          git add $LOG && git commit -m "ci: trade check" || true
          git push "https://x-access-token:${GH_PAT}@github.com/sacredhealing/sacredhealing-fa5d6004.git" main || true
