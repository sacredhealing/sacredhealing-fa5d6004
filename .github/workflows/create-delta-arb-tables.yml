name: Create Delta-Arb Tables Direct

on:
  workflow_dispatch:

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ADMIN_PAT }}
      - name: Run table creation script
        env:
          SUPABASE_PAT: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          GH_PAT: ${{ secrets.GH_ADMIN_PAT }}
        run: |
          python3 scripts/create_delta_arb.py 2>&1 | tee _delta_arb_setup.txt
          git config user.email "bot@sqi.app"; git config user.name "SQI-Bot"
          git add _delta_arb_setup.txt && git commit -m "ci: delta-arb setup result" || true
          git push "https://x-access-token:${GH_PAT}@github.com/sacredhealing/sacredhealing-fa5d6004.git" main || true
