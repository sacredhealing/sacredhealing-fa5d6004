/**
 * Barrel for the conversion upgrade modal — canonical implementation is in
 * `conversion/ConversionSystem.tsx` (ConversionProvider + MusicPlayer hook-up).
 */
export {
  UpgradeModal,
  useUpgradeModal,
  UPGRADE_MODAL_STORAGE_KEY,
} from "@/components/conversion/ConversionSystem";
